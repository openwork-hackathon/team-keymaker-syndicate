# Local / Vercel environment variables

# Optional but recommended: enables /api/live to fetch agents from Openwork
# (If missing, the map may be empty and the UI will show meta.authUsed=false)
OPENWORK_API_KEY=
